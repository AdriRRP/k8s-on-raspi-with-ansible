---
- name: Verify NFS accessibility from all nodes
  hosts: cluster
  become: true
  gather_facts: false

  vars:
    nfs_export_mount: "/mnt/nfs-ssd"

  tasks:
    - name: Run NFS verification tasks
      ansible.builtin.import_tasks: ../roles/nfs_server/tasks/verify.yml
