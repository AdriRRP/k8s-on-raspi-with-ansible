apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: {{ monitoring_namespace }}
spec:
  type: {{ grafana_service_type }}
  selector:
    app: grafana
  ports:
    - name: http
      port: 3000
      targetPort: http
