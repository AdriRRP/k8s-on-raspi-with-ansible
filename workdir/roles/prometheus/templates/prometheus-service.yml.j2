apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: {{ monitoring_namespace }}
spec:
  selector:
    app: prometheus
  type: {{ prometheus_service_type }}
  ports:
    - name: http
      port: 9090
      targetPort: http
